## Codebase Patterns
- Parent chapter intro slides should be added as a group-level `overview` object in `src/data/slides.js`; the shared overview renderer in `src/App.jsx` already maps that shape into the standard chapter layout.
- Child slides can be filled into `src/data/slides.js` incrementally through `childContent`; missing later entries safely render the shared unfinished-slide placeholder until their story is implemented.
- Historical/context subslides can reuse the `ledger` layout when they read cleanly as "before vs proposal" plus a short dated sequence, so simple origin stories do not need custom timeline components.
- Pending-versus-confirmed lifecycle slides also fit the `ledger` layout well: use the two comparison cards for state before and after confirmation, and the side flow for the broadcast-to-inclusion path.
- Structural explainer slides can use the shared `anatomy` layout with `frameLabel`, `segments`, and `notes` when a concept needs its internal parts, roles, or consequences separated visually without adding slide-specific React code.
- Cause-and-effect security slides can use a shared `hash-chain` layout with `chain` and `tamperedChain` arrays in `src/data/slides.js`; the renderer can show an intact path and a broken reference without bespoke slide components.
- Monetary-policy slides can use the shared `issuance` layout with `process`, `eras`, `stats`, and `notes` when one slide needs both a protocol workflow and a stepped reward or supply schedule.
- The shared `issuance` layout can also explain a fixed cap if `eras` are framed as cumulative supply milestones; the same bar treatment reads well for "approaching the ceiling" stories.
- Wallet mental-model slides can reuse the shared `anatomy` layout by splitting the explanation into public-side details, private-key control, and wallet-software responsibilities.

# Ralph Progress Log
Started: Sun Apr 19 12:21:13 CEST 2026
---

## 2026-04-19 12:32 CEST - SLIDE-02
- Implemented the Bitcoin chapter intro as a presentation-ready parent slide using the shared overview layout, framing Bitcoin as the first major blockchain application and outlining the chapter arc.
- Files changed: `src/data/slides.js`, `prd.json`, `progress.txt`, `AGENTS.md`
- **Learnings for future iterations:**
  - Parent chapter slides do not need custom React work when the standard chapter structure is enough; add `overview` content to the slide group and let the shared renderer pick it up.
  - Root `AGENTS.md` is the right place to preserve deck-wide authoring conventions that apply across future slide stories.
  - Browser verification is still manual in this environment; use `npm run build` for a fast regression check, then note that a human browser pass is still needed.
---

## 2026-04-19 13:47 CEST - SLIDE-02-1
- Implemented slide 02.1 as a shared-layout explainer covering digital scarcity, the double-spend problem, and why trusted intermediaries keep the master ledger in traditional online payments.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-WKr2KGmH.js`; removed stale build asset `dist/assets/index-ATKB0yGI.js`
- **Learnings for future iterations:**
  - The existing `ledger` slide layout works for more than blockchain-mechanics slides; it also fits “problem / comparison / sequence” explainers without adding new React code.
  - `npm run build` is the only automated quality gate currently configured, and browser verification still needs a manual pass outside this environment.
  - Building the deck updates tracked `dist/` artifacts, so include the regenerated hashed bundle and removed stale bundle in the same commit.
---
## 2026-04-19 13:49 CEST - SLIDE-02-2
- Implemented slide 02.2 as a Bitcoin origin-story explainer covering Satoshi Nakamoto, the October 31, 2008 white paper, the move from trusted intermediaries to protocol rules, and the January 2009 launch sequence.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-OOlvN7BW.js`; removed stale build asset `dist/assets/index-WKr2KGmH.js`
- **Learnings for future iterations:**
  - Historical context slides can still use the shared `ledger` layout if the comparison is framed as "before the proposal" versus "what the proposal changed" and the side panel carries the dated sequence.
  - Use exact dates sparingly on historically anchored slides when they help orient the audience without turning the slide into a biography.
  - Browser verification is still manual in this environment; `npm run build` remains the only automated quality check available here.
---
## 2026-04-19 13:52 CEST - SLIDE-02-3
- Implemented slide 02.3 as a block-anatomy explainer that separates ordered transactions, header metadata, and block identity so the next hashing and mining slides have a concrete mental model to build on.
- Files changed: `AGENTS.md`, `src/App.jsx`, `src/data/slides.js`, `src/styles.css`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-BLt89fn4.css`, `dist/assets/index-BhboGCNT.js`; removed stale build assets `dist/assets/index-B_p6VGVT.css` and `dist/assets/index-OOlvN7BW.js`
- **Learnings for future iterations:**
  - The shared `anatomy` layout is a good fit when one slide needs to explain the internal parts of a single object rather than compare two systems or show a process sequence.
  - Keeping the layout generic in `src/App.jsx` and driving it from `segments` in `src/data/slides.js` makes it reusable for later slides without bespoke component work.
  - `npm run build` passed after the change; browser verification is still manual in this environment.
---
## 2026-04-19 13:53 CEST - SLIDE-02-4
- Implemented slide 02.4 as a hash-link explainer that shows a valid three-block chain first, then a tampered middle block that changes its hash and breaks the next block's stored reference.
- Files changed: `AGENTS.md`, `src/App.jsx`, `src/data/slides.js`, `src/styles.css`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-W9nIsY-6.css`, `dist/assets/index-C-3jAm-n.js`; removed stale build assets `dist/assets/index-BLt89fn4.css` and `dist/assets/index-BhboGCNT.js`
- **Learnings for future iterations:**
  - The shared `hash-chain` layout works well for slides that need to compare a valid chain against a tampered one without introducing motion.
  - Driving both the intact and broken rows from slide data keeps the React renderer reusable for later security or consensus explainers.
  - `npm run build` passed; browser verification is still manual in this environment.
---
## 2026-04-19 13:57 CEST - SLIDE-02-5
- Implemented slide 02.5 as a shared-layout Bitcoin explainer pairing the mining flow with a stepped halving schedule, plus supporting stats and beginner-friendly notes about issuance and fees.
- Files changed: `AGENTS.md`, `src/App.jsx`, `src/data/slides.js`, `src/styles.css`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-BFby-P-C.css`, `dist/assets/index-B4UuHBaW.js`; removed stale build assets `dist/assets/index-W9nIsY-6.css` and `dist/assets/index-C-3jAm-n.js`
- **Learnings for future iterations:**
  - The shared `issuance` layout works when a slide needs to explain both a protocol process and a shrinking reward or supply schedule without resorting to a one-off component.
  - Keeping the reward-era bars data-driven in `src/data/slides.js` makes it easy to reuse the same renderer for later Bitcoin supply or Ethereum issuance comparisons.
  - `npm run build` passed; browser verification is still manual in this environment.
---
## 2026-04-19 13:59 CEST - SLIDE-02-6
- Implemented slide 02.6 as a Bitcoin supply-cap explainer using the shared issuance layout to show how halvings shrink new issuance and make cumulative supply approach 21 million BTC.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-QZOVW5O0.js`; removed stale build asset `dist/assets/index-B4UuHBaW.js`
- **Learnings for future iterations:**
  - The shared `issuance` layout is flexible enough for both per-era reward slides and cumulative-supply slides; changing the semantic meaning of `eras` can avoid extra React work.
  - For fixed-cap explanations, focus the copy on flow versus stock: halvings reduce new issuance, while the hard cap describes the eventual total supply.
  - `npm run build` passed; browser verification is still manual in this environment.
---
## 2026-04-19 14:01 CEST - SLIDE-02-7
- Implemented slide 02.7 as a Bitcoin decentralization explainer using the shared anatomy layout, covering node rule enforcement, the distinction between nodes and miners, and why broad node distribution supports censorship resistance and resilience.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-IYO3GbAN.js`; removed stale build asset `dist/assets/index-QZOVW5O0.js`
- **Learnings for future iterations:**
  - The shared `anatomy` layout also works for role-based concept slides where each segment explains a different responsibility or consequence, not just the physical parts of one object.
  - For decentralization topics, anchor the explanation in operational roles first, then connect distribution to censorship resistance and uptime so the concept stays concrete.
  - Automated verification still relies on `npm run build`; browser verification remains a manual follow-up in this environment.
---
## 2026-04-19 14:02 CEST - SLIDE-02-8
- Implemented slide 02.8 as a Bitcoin wallet explainer using the shared anatomy layout, separating the public side of receiving, the private key's signing role, and the wallet app's job as a key manager rather than a coin container.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-hQFR9SqI.js`; removed stale build asset `dist/assets/index-IYO3GbAN.js`
- **Learnings for future iterations:**
  - Wallet slides read better when they explicitly separate blockchain state from wallet software so beginners do not assume coins live inside the app.
  - The shared `anatomy` layout is a good fit for wallet and key mental models because each segment can own one responsibility in the receive/sign/manage flow.
  - `npm run build` passed; browser verification still needs a manual follow-up in this environment.
---
## 2026-04-19 14:10 CEST - SLIDE-02-9
- Implemented slide 02.9 as a Bitcoin mempool explainer using the shared ledger layout, contrasting pending versus confirmed state and showing the path from wallet broadcast to node mempool to miner selection to confirmations.
- Files changed: `AGENTS.md`, `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-BgJdMKNS.js`; removed stale build asset `dist/assets/index-hQFR9SqI.js`
- **Learnings for future iterations:**
  - Pending-versus-confirmed lifecycle topics can use the shared `ledger` layout effectively when the main teaching goal is state transition plus a short protocol flow.
  - For mempool slides, keep the distinction explicit: nodes verify and relay valid pending transactions, while miners select from that pool when building blocks.
  - `npm run build` passed; browser verification is still manual in this environment.
---
## 2026-04-19 14:33 CEST - SLIDE-03
- Implemented the Ethereum chapter intro as a shared overview slide, framing Ethereum as the shift from blockchain as money infrastructure to blockchain as a programmable state machine and previewing the accounts, EVM, and smart contract topics.
- Files changed: `src/data/slides.js`, `prd.json`, `progress.txt`, `dist/index.html`, `dist/assets/index-CNY0yA9z.js`; removed stale build asset `dist/assets/index-BgJdMKNS.js`
- **Learnings for future iterations:**
  - Chapter intro slides stay clean when the three pillars cover capability shift, mental model, and chapter roadmap rather than trying to teach subtopics too early.
  - The existing overview schema is sufficient for new chapter intros; if the story is a parent slide, default to slide-data changes before considering React edits.
  - `npm run build` passed; browser verification still needs a manual follow-up in this environment.
---
